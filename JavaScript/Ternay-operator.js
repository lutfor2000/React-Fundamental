const age = 20
let type;

if(age >= 18){
    type = "Adult"
}else{
    type = "Child"
}

console.log(type)

//--Ternary Operator
const age = 10

const rasult = age >= 18 ? "Adult" : age >= 7 ? "Infant" : "Child";

console.log(rasult)


