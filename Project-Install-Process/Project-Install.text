at first this cmd then Hosting file upload 
-> npm run build

Vite-Install - npm create vite@latest -> Project Name -> React -> Select language JavaScript -> yes 

===============================================================================================================================
Installing Tailwind Css ==> 
-> npm install tailwindcss @tailwindcss/vite

==> vite.config.js File Setup

import tailwindcss from '@tailwindcss/vite'
export default defineConfig({
  plugins: [react(),tailwindcss()],
})
index.css file add -> @import "tailwindcss";
===============================================================================================================================

------------------------------Framework Guides-------------------------


✅ 1. Tailwind install করো
npm install -D tailwindcss postcss autoprefixer

✅ 2. Tailwind config generate করো
npx tailwindcss init -p

-----Or--------
# ৩. Tailwind ইনস্টল করুন (ভারশন স্পেসিফিক করে)
npm install --save-dev tailwindcss@3.4.0 postcss@8.4.31 autoprefixer@10.4.16
npx tailwindcss init -p

->create two file->
tailwind.config.js
postcss.config.js

---------->tailwind.config.js<--------
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: '#E58411',
        "primary-light": "#E58411",
      },
    },
  },
  plugins: [],
}

index.css

✅ 3. CSS file-এ Tailwind add করো
👉 src/index.css (বা main css file) এ লিখো:
@tailwind base;
@tailwind components;
@tailwind utilities;

tailwind.config.js এ তোমার code বসাও (আগে যে fixed version দিয়েছি)

✅ 5. Project run করো
npm run dev
===============================================================================================================================

=> daisyUI installing -> npm i -D daisyui@latest
=> index.css file add -> @plugin "daisyui"; / @import "daisyui/daisyui.css";

==============================================================================================================================

=> Installing React Icon ->
CMD -> npm install react-icons --save
